Where "dig deeper" lives without leaving the view — methodology, column config, quick filters.

```jsx
<Popover title="How this is calculated" trigger={<IconButton icon="info" label="Method" size="sm" />}>
  Beta-adjusted using a 60-day rolling regression against the primary benchmark.
</Popover>
```
