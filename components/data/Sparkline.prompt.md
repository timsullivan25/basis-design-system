Shape without axes — always sits beside the number it describes, never alone.

```jsx
<Sparkline data={nav} width={80} height={22} baseline />
<Sparkline data={series} color="var(--chart-2)" area={false} width={56} height={16} />
```

72×20 in table cells, 80–120×24 in KPI cards.
