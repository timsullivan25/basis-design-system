Multi-select and row selection. `indeterminate` for a group header whose children are partly selected.

```jsx
<Checkbox checked={sel} onChange={setSel} label="Include cash" description="Treats FX sweep as a position" />
```
