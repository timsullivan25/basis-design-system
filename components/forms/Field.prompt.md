Wraps any control with a micro-caps label, optional hint glyph and error line.

```jsx
<Field label="Benchmark" hint="Used for active-return attribution">
  <Select options={[{value:'spx',label:'S&P 500'}]} />
</Field>
```

Use `inline` in settings panes so labels align in a 120px column.
