Pill for things the *user* set — active filters, sector labels, series legends.

```jsx
<Tag icon="filter" onRemove={clear}>Sector: Energy</Tag>
<Tag color="var(--chart-2)">Momentum L/S</Tag>
```

Rounded-pill, unlike Badge's square status pill — the shape difference tells users what they can remove.
