Icon-only control for dense chrome — toolbars, table row ends, panel headers.

```jsx
<IconButton icon="settings-2" label="Column settings" size="sm" />
<IconButton icon="star" label="Watch" variant="outline" selected />
```

`ghost` by default; use `outline` when it sits alone on a light surface. Never ship one without `label`.
