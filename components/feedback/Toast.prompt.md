Transient confirmation of a background action. Dark surface — the only inverse component in normal use.

```jsx
<Toast tone="positive" title="Screen saved" action={<Button variant="link" size="xs">Open</Button>}>
  42 names matched your criteria.
</Toast>
```
