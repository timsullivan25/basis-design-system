Focused task or confirmation. Never for exploring data — that is a drill-in panel or a route.

```jsx
<Dialog open={open} onClose={close} title="New screen" icon="filter" subtitle="Saved to your workspace"
  footer={<><Button onClick={close}>Cancel</Button><Button variant="primary">Create</Button></>}>
  …
</Dialog>
```
