Inline message tied to the view. Use `compact` for the one-liner above a table.

```jsx
<Alert tone="caution" compact title="Prices delayed 15 min"
  actions={<Button variant="link" size="xs">Reconnect</Button>} />
```
